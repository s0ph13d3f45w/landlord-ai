<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Restablecer Contraseña - Landlord AI</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <div class="auth-container">
    <div class="auth-box">
      <h1>🏠 Landlord AI</h1>
      <h2>Crea una nueva contraseña</h2>
      
      <% if (error) { %>
        <div class="error-message"><%= error %></div>
      <% } %>
      
      <% if (token) { %>
        <form action="/reset-password" method="POST">
          <input type="hidden" name="token" value="<%= token %>">
          <input type="password" name="password" placeholder="Nueva Contraseña" required minlength="6">
          <input type="password" name="confirm_password" placeholder="Confirmar Contraseña" required minlength="6">
          <button type="submit">Restablecer Contraseña</button>
        </form>
      <% } else { %>
        <p style="text-align: center; margin: 20px 0;">
          <a href="/forgot-password" class="btn-primary" style="text-decoration: none; padding: 12px 24px; display: inline-block;">Solicitar Nuevo Enlace</a>
        </p>
      <% } %>
      
      <p class="auth-link">
        <a href="/login">← Volver al inicio de sesión</a>
      </p>
    </div>
  </div>
</body>
</html>